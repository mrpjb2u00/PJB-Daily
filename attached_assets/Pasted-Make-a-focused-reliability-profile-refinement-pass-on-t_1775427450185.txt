Make a focused reliability + profile refinement pass on the current app. Build on top of the current working version. Do not redesign the whole app.

IMPORTANT:

* Keep all current navigation and functionality intact
* Do not break task creation/editing
* Do not break note creation/editing
* This pass has two goals only:

  1. Improve the Profile screen with useful real counts
  2. Add draft autosave protection for Notes and Task Editor

PART 1 — PROFILE SCREEN COUNT CARDS

Upgrade the current Profile screen by adding a small stats row under the profile header.

Requirements:

* Show real counts, not fake data
* Add:

  * Open To-Dos count
  * Notes count
* Keep styling clean and consistent with the current Profile screen
* Use compact cards or stat boxes
* Do not add fake premium/status badges
* Do not add charts

Definitions:

* Open To-Dos = active/incomplete todos
* Notes count = total notes

PART 2 — DRAFT AUTOSAVE FOR NOTES

Add draft autosave protection for the Note Editor.

Requirements:

* While the user is typing a new or edited note, autosave the draft after a short pause in typing (around 1–2 seconds of inactivity)
* Also save draft state if the app goes to background or the user navigates away
* When the user returns to the note editor, restore the unsaved draft if one exists
* Make this safe and non-destructive
* Do not create duplicate notes
* If the user explicitly saves/submits the note, clear the draft state properly

PART 3 — DRAFT AUTOSAVE FOR TASK EDITOR

Add draft autosave protection for the Task Editor / New Task screen.

Requirements:

* Preserve unsaved input for:

  * title
  * details
  * date
  * repeat
  * priority
  * subtasks draft state if currently present in the UI
* Autosave after a short pause in typing or changes
* Also save when the app backgrounds or the user navigates away
* Restore unfinished task draft when the user returns
* Do not automatically create a real task until the user explicitly presses Add/Save
* Clear the draft after successful Add/Save or explicit discard/cancel if appropriate

PART 4 — UX SAFETY

* Keep autosave lightweight and reliable
* Avoid lag or excessive writes
* Do not change the existing save/add behavior
* If helpful, show a subtle “Draft restored” or similar message when reopening a saved draft, but keep it minimal

DO NOT DO THESE YET:

* Do not redesign the Note Editor visually yet
* Do not redesign the Profile screen again beyond the small stat row
* Do not add backend sync for drafts
* Do not add notifications
* Do not add fake analytics

WHEN FINISHED:
Summarize exactly:

1. what real counts were added to Profile
2. how Note draft autosave works
3. how Task draft autosave works
4. when drafts are cleared
5. what files were updated
6. what I should manually test next
